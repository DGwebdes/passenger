# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules/
dist/
dist-ssr
*.local
.env
test/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sentry Config File
.env.sentry-build-plugin
infra/terraform/terraform.tfvars

# Local .terraform directories (downloaded provider plugins, modules cache)
**/.terraform/*

# State files — contain full resource details, sometimes including secrets
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# All .tfvars files — likely to contain project-specific or sensitive values
*.tfvars
*.tfvars.json

# Local override files (used for local-only resource overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Saved plan output, if you ever run `terraform plan -out=tfplan`
*.tfplan

# CLI config files
.terraformrc
terraform.rc
